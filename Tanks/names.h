#pragma once
#include<string>

namespace Names {
	enum tank_color { blue, dark, green, red, sand };
	enum barrel_size { big, medium, small };

	tank_color color_to_enum(const std::string& str) {
		tank_color color;
		switch (str) {
		}
	}
}